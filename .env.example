# Environment Configuration for Cloud Run Deployment
# Copy this file to .env and update the values as needed

# Google Cloud Project Configuration
GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Google Cloud Storage Configuration
GCS_BUCKET_NAME=aigameschat-game-data

# Application Configuration
PORT=8080
ENVIRONMENT=production

# FastAPI Configuration
FASTAPI_DEBUG=false
FASTAPI_RELOAD=false

# CORS Configuration (adjust as needed for production)
ALLOWED_ORIGINS=*
ALLOWED_METHODS=*
ALLOWED_HEADERS=*

# Logging Configuration
LOG_LEVEL=INFO

# Security Configuration
# Add any API keys or secrets here (these should be set as Cloud Run environment variables)
# Never commit actual secrets to version control!
# API_KEY=your-api-key-here
# SECRET_KEY=your-secret-key-here

# Google Cloud Authentication
# When running on Cloud Run, authentication is handled automatically
# For local development, you may need to set:
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json